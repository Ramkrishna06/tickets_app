Explaination about why constructure is called in main method.

copwith function
=copywith function allows to create another instance of class
=depection of method in main functio Test case.


layout building(layoutbuilder -flex-Listgenerator-index(sizebox))


final and const concept==if somthing is final that is it cant be modified after once initilise so we can add
const(6:00:00)